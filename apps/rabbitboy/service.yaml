apiVersion: v1
kind: Service
metadata:
  name: rabbitboy-static
  namespace: rabbitboy
spec:
  selector:
    app: rabbitboy-static
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP